# ============================================
# You.com API Configuration
# ============================================
# Get your API key from: https://api.you.com
# Documentation: https://documentation.you.com
#
# IMPORTANT: API key is ONLY used by the backend FastAPI server
# - Frontend components call backend endpoints at /api/v1/*
# - Backend server uses this key to call You.com APIs
# - This keeps the API key secure and never exposes it to the browser
# - All API logic, rate limiting, and caching happens on the backend

# Backend API Key (Python/FastAPI - REQUIRED)
YOU_API_KEY=your_you_api_key_here

# Frontend API Key (Next.js - for client-side fallback)
NEXT_PUBLIC_YOU_API_KEY=your_you_api_key_here

# Database Configuration (matches docker-compose exposed ports)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5433/cia_hackathon

# Redis Configuration
REDIS_URL=redis://localhost:6380

# Security
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Environment
ENVIRONMENT=development

# Demo Mode (set to true to use mock data instead of real API calls)
DEMO_MODE=false

# Email Configuration (for automated alerts and sharing)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password
FROM_EMAIL=noreply@enterprisecia.com

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:8765

# SSO Configuration - Week 1 Implementation
# Google OAuth2 (get from Google Cloud Console)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# Azure AD OAuth2 (get from Azure Portal)
AZURE_CLIENT_ID=your_azure_client_id
AZURE_CLIENT_SECRET=your_azure_client_secret
AZURE_TENANT_ID=your_azure_tenant_id
AZURE_REDIRECT_URI=http://localhost:3000/auth/azure/callback

# Okta OAuth2 (get from Okta Developer Console)
OKTA_CLIENT_ID=your_okta_client_id
OKTA_CLIENT_SECRET=your_okta_client_secret
OKTA_DOMAIN=your_okta_domain.okta.com
OKTA_REDIRECT_URI=http://localhost:3000/auth/okta/callback

# Microsoft Teams Integration - Week 2 Implementation
# Get from Microsoft Teams Developer Portal
TEAMS_BOT_TOKEN=your_teams_bot_token
TEAMS_APP_ID=your_teams_app_id
TEAMS_APP_PASSWORD=your_teams_app_password

# Frontend URL for notifications and links
FRONTEND_URL=http://localhost:3000
